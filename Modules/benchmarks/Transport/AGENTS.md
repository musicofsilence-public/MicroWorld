# Transport Benchmarks

Inherits `../../AGENTS.md`.

## Architecture

Transport benchmark evidence (inherited from the Net package). `Results/` holds
the host and ESP32-S3 compile margins for the byte-I/O and host layer.

## Concepts

Measured margins live in `Results/Host.md` and `Results/Esp32S3N16R8.md` and
are indexed by `../../docs/ResourceBudgets.md`.

## Verification

See `Results/` for the recorded evidence anchored to its source commit.
