# Core Benchmarks

Inherits `../../AGENTS.md`.

## Architecture

Core benchmark evidence. The dispatch benchmark source was retired with the
Core actor model; `Results/` holds the host and ESP32-S3 compile margins for
the lifecycle/tick/memory surface.

## Concepts

Measured margins live in `Results/Host.md` and `Results/Esp32S3N16R8.md` and
are indexed by `../../docs/ResourceBudgets.md`.

## Verification

See `Results/` for the recorded evidence anchored to its source commit.
